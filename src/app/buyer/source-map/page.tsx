import BuyerSourceMapClient from '@/components/buyer/source/BuyerSourceMapClient';

/**
 * The buyer source map page containing a map and an analytics sidebar.
 * @returns The buyer source map client
 */
export default function BuyerSourceMap() {
  return <BuyerSourceMapClient></BuyerSourceMapClient>;
}
