import BrowseProduceClient from '@/components/buyer/produce/BrowseProduceClient';

/**
 * The buyer browse produce page containing a list view and map view.
 * @returns The buyer browse produce client
 */
export default function BuyerGrowers() {
  return <BrowseProduceClient></BrowseProduceClient>;
}
