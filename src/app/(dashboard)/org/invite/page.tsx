import OrgInviteClient from '@/components/organization/members/OrgInviteClient';

/**
 * The org invite page containing an invite form and an invite table.
 * @returns The org invite client
 */
export default function OrgInvite() {
  return <OrgInviteClient />;
}
