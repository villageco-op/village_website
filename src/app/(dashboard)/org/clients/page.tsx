import ClientsPageClient from '@/components/organization/clients/ClientsPageClient';

/**
 * The org clients page containing a table of clients and controls.
 * @returns The org clients page client
 */
export default function OrgClients() {
  return <ClientsPageClient />;
}
