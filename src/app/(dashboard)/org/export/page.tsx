import ClientsExportClient from '@/components/organization/export/ClientsExportClient';

/**
 * The page with buttons for downloading and exporting an orgs clients.
 * @returns The export clients client
 */
export default function ClientExport() {
  return <ClientsExportClient />;
}
