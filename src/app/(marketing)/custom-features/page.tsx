import CustomFeaturesContactClient from '@/components/contact/CustomFeaturesContactClient';

/**
 * The request custom features contact page.
 * @returns The contact page client
 */
export default function CustomFeaturePage() {
  return <CustomFeaturesContactClient />;
}
