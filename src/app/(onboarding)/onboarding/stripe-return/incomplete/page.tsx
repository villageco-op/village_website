import StripeReturnIncompletePage from '@/components/onboarding/individual/StripeReturnIncomplete';

/**
 * The page for handling Stripe returning early from connecting. Lets the user retry.
 * @returns The Stripe return incomplete page
 */
export default function StripeReturnIncomplete() {
  return <StripeReturnIncompletePage />;
}
