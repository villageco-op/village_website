import StripeRefreshErrorPage from '@/components/onboarding/individual/StripeRefreshError';

/**
 * The page for handling Stripe refresh errors. Lets the user retry.
 * @returns The Stripe refresh error page
 */
export default function StripeRefreshError() {
  return <StripeRefreshErrorPage />;
}
